apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-service
  namespace: jobmgmt
spec:
  replicas: 2
  selector:
    matchLabels:
      app: auth-service
  template:
    metadata:
      labels:
        app: auth-service
    spec:
      containers:
        - name: auth-service
          image: ghcr.io/samaresh-16/auth-service:fc5e82de053763ec66822078c0c41dd69437afa8
          ports:
            - containerPort: 8080

---
apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: jobmgmt
spec:
  type: NodePort
  selector:
    app: auth-service
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080
